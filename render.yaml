services:
  - type: web
    name: la-scarpetta-ochtrup-concept
    runtime: node
    region: frankfurt
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
    autoDeploy: true
